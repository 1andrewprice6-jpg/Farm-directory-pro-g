import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.farmdirectoryprog',
  appName: 'FarmDirectoryProG',
  webDir: 'dist'
};

export default config;
